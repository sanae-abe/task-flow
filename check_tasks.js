console.log('=== タスクデータ確認 ==='); const data = localStorage.getItem('kanban-boards'); if (data) { const boards = JSON.parse(data); boards.forEach(board => { console.log('Board:', board.title); board.columns.forEach(column => { column.tasks.forEach(task => { if (task.recurrence) { console.log('Task with recurrence:', task.title, task.recurrence); } }); }); }); }); } else { console.log('No kanban data found'); }
