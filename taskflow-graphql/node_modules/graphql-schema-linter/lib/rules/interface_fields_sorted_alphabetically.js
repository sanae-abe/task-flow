"use strict";

Object.defineProperty(exports, "__esModule", {
  value: true
});
exports.InterfaceFieldsSortedAlphabetically = InterfaceFieldsSortedAlphabetically;

var _validation_error = require("../validation_error");

var _listIsAlphabetical = _interopRequireDefault(require("../util/listIsAlphabetical"));

function _interopRequireDefault(obj) { return obj && obj.__esModule ? obj : { default: obj }; }

function InterfaceFieldsSortedAlphabetically(configuration, context) {
  return {
    InterfaceTypeDefinition(node) {
      const ruleKey = 'interface-fields-sorted-alphabetically';
      const fieldList = (node.fields || []).map(field => field.name.value);
      const {
        sortOrder = 'alphabetical'
      } = configuration.getRulesOptions()[ruleKey] || {};
      const {
        isSorted,
        sortedList
      } = (0, _listIsAlphabetical.default)(fieldList, sortOrder);

      if (!isSorted) {
        context.reportError(new _validation_error.ValidationError('interface-fields-sorted-alphabetically', `The fields of interface type \`${node.name.value}\` should be sorted in ${sortOrder} order. ` + `Expected sorting: ${sortedList.join(', ')}`, [node]));
      }
    }

  };
}