# ============================================================================
# TaskFlow GraphQL Server - Environment Variables Example
# ============================================================================

# Server Configuration
PORT=4000
NODE_ENV=development

# ============================================================================
# AI Integration (Week 8)
# ============================================================================

# Enable AI features (true/false)
AI_API_ENABLED=false

# AI Provider (openai, anthropic, etc.)
AI_PROVIDER=openai

# OpenAI Configuration
# Get your API key from: https://platform.openai.com/api-keys
AI_API_KEY=your_openai_api_key_here

# OpenAI Model (default: gpt-4o-mini)
# Options: gpt-4o-mini, gpt-4o, gpt-4-turbo, gpt-3.5-turbo
AI_MODEL=gpt-4o-mini

# ============================================================================
# IP Geolocation (Week 8)
# ============================================================================

# IP Geolocation API Key (optional - uses free ipapi.co if not set)
# Get your API key from: https://ipgeolocation.io/
# Free tier: 1,000 requests/day (ipapi.co, no key required)
# Premium: 30,000 requests/month (ipgeolocation.io, requires key)
IP_GEOLOCATION_API_KEY=

# ============================================================================
# Redis Configuration (Week 8)
# ============================================================================

# Enable Redis features (true/false)
REDIS_ENABLED=false

# Redis connection URL (takes precedence over individual settings)
REDIS_URL=redis://localhost:6379

# Individual Redis connection settings (used if REDIS_URL is not set)
REDIS_HOST=localhost
REDIS_PORT=6379
REDIS_PASSWORD=
REDIS_DB=0

# Rate Limiting Configuration
# Maximum requests per window (default: 100 requests per minute)
RATE_LIMIT_MAX_REQUESTS=100
RATE_LIMIT_WINDOW_SECONDS=60

# ============================================================================
# Logging Configuration (Week 8)
# ============================================================================

# Log level (debug, info, warn, error, fatal)
LOG_LEVEL=info

# Sentry DSN for error tracking (optional)
# SENTRY_DSN=your_sentry_dsn_here

# ============================================================================
# TODO.md Sync Configuration (Phase 0)
# ============================================================================

# Path to TODO.md file (relative to project root)
TODO_MD_PATH=../TODO.md

# Debounce time in milliseconds (default: 500ms)
TODO_DEBOUNCE_MS=500

# Throttle time in milliseconds (default: 2000ms)
TODO_THROTTLE_MS=2000

# Maximum file size in MB (default: 5MB)
TODO_MAX_FILE_SIZE_MB=5

# Maximum number of tasks (default: 10000)
TODO_MAX_TASKS=10000

# Enable webhooks for TODO.md sync (true/false)
TODO_WEBHOOKS_ENABLED=false

# MCP Authentication Token (generate with: openssl rand -base64 32)
# Required for MCP Tool access
MCP_AUTH_TOKEN=your_32_char_auth_token_here
